export type ChangeMyUserPasswordBodyDtoType = {
  /**
   * @description The old user password
   * @type string
   */
  oldPassword: string
  /**
   * @description The new user password
   * @type string
   */
  newPassword: string
}
