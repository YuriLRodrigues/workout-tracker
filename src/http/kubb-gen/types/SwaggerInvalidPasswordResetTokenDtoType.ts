export type SwaggerInvalidPasswordResetTokenDtoType = {
  /**
   * @default "Invalid password to reset token"
   * @type string
   */
  message: string
  /**
   * @default 401
   * @type number
   */
  statusCode: number
}
